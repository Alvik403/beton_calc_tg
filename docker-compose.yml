version: "3.9"

services:
  bot:
    build: .
    env_file:
      - .env
    command: python -m app.main
    restart: unless-stopped

  web:
    build: .
    env_file:
      - .env
    command: uvicorn app.web:app --host 0.0.0.0 --port 8000
    ports:
      - "8000:8000"
    restart: unless-stopped