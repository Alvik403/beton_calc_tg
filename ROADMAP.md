# План обновлений (Roadmap)

## В работе / Ближайшие

- [x] **Excel для ЖБИ** — поддержка `mode=excel` для направления jbi; выгрузка в .xlsx (как у бетона: лист «Итог», четыре варианта цен)
- [ ] **Тесты на directions** — проверка `get_all_directions()`, `get_direction()`, `validate_scope()`
- [ ] **Тесты на upload с разными scope** — beton, jbi, fallback

## Будущие улучшения

- [ ] **Третий и последующие направления** — доработка UI: JS для динамических ID (`form-{id}`, `cfgBtn-{id}` и т.д.) вместо legacy_ids
- [ ] **Универсальный рендер результатов** — заменить хардкод «Расчет ЖБИ» / «Расчет бетона» на вывод по `direction.display_name`
- [ ] **Перевод бота на directions** — бот сейчас использует только beton; возможность выбора направления через команду или тип документа
- [ ] **Хранение конфигурации** — вынос `CONFIG_PASSWORD` в `.env`; опционально база для профилей
- [ ] **Логирование и мониторинг** — structured logs; health-check для Redis/Celery
- [ ] **Документация API** — OpenAPI/Swagger с примерами; описание полей ответа upload

## Идеи

- [ ] **Расширяемые источники бетона** — не только beton, но и несколько направлений как источник для units
- [ ] **Импорт/экспорт профилей** — JSON/YAML для обмена настройками
- [ ] **Пакетная обработка** — несколько файлов за раз
- [ ] **Валидация Excel** — предпросмотр/проверка формата до расчёта
- [ ] **История расчётов** — сохранение последних job_id, быстрый повтор
